## 19. Open Decisions

- Whether to expose `api.schema.json` in the first implementation wave or after DTOs stabilize.
- When to introduce `config.schema.json`, `api.schema.json`, and `events.schema.json`; root `schema.json` remains the config schema for now.
- Whether the first UI target is TUI-only or Native HTTP API should come up in the same milestone.
- Which provider adapter is the first executable path: OpenAI Responses, OpenAI Chat Completions, or OpenAI-compatible Chat Completions.
